// MATHS-LOGIC.JS
const LESSONS_DATA = [
    { id: 1, title: "Multiplications", emoji: "✖️", xp: 50, questions: [...] },
    // Colle ici tout le tableau LESSONS_DATA et toutes les fonctions JS
];

function initMaths() {
    updateXPDisplay();
    renderLessons();
}

document.addEventListener('DOMContentLoaded', initMaths);